/** Add Item — insert a new blank item after the item with the given id. */
export function addItem(id: string | null): void {
  console.log('[action] add-item after', id);
  // TODO: append new MnItem after this item's position in the section
}
