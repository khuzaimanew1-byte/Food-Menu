/** Add Section (from item context) — insert a new section after the section
 *  that contains the item with the given id. */
export function addSectFromItem(id: string | null): void {
  console.log('[action] add-section from item', id);
  // TODO: create new MnHdg section after the parent section of this item
}
