/** Delete Section — remove the section with the given id after confirmation.
 *  Also removes all items inside it. */
export function deleteSection(id: string | null): void {
  console.log('[action] delete-section', id);
  // TODO: confirm dialog → remove section + its items from data store
}
