/** Shape: Infinity Castle — set avatar shape to Infinity Castle crown (ic)
 *  for all items on the page with the given id. */
export function shapeInfCastle(id: string | null): void {
  console.log('[action] shape-infinity-castle on page', id);
  // TODO: apply shape="ic" to all MnItm on this page
}
