/** Shape: Plaque — set avatar shape to plaque for all items on the page
 *  with the given id. */
export function shapePlaque(id: string | null): void {
  console.log('[action] shape-plaque on page', id);
  // TODO: apply shape="plq" to all MnItm on this page
}
