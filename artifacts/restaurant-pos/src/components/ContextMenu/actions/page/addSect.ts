/** Add Section — append a new blank section to the page with the given id (page number). */
export function addSectToPage(id: string | null): void {
  console.log('[action] add-section to page', id);
  // TODO: create new MnHdg section at end of this page's section list
}
