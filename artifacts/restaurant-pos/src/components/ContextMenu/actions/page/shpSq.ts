/** Shape: Square — set avatar shape to square for all items on the page
 *  with the given id, or toggle a square decorative shape on the page. */
export function shapeSquare(id: string | null): void {
  console.log('[action] shape-square on page', id);
  // TODO: apply shape="sq" to all MnItm on this page
}
