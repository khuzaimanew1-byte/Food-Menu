/** Move Item — enter drag-to-reorder mode for the item with the given id. */
export function moveItem(id: string | null): void {
  console.log('[action] move-item', id);
  // TODO: activate drag-reorder handle / open move-to picker
}
