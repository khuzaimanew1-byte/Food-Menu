/** Assign — open category/section picker to reassign the item with the given id. */
export function assignItem(id: string | null): void {
  console.log('[action] assign-item', id);
  // TODO: open section picker modal; move item to chosen section
}
