/** Delete Item — remove the item with the given id after confirmation. */
export function deleteItem(id: string | null): void {
  console.log('[action] delete-item', id);
  // TODO: confirm dialog → remove item from data store
}
