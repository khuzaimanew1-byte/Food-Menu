/** Add Section (from section context) — insert a new blank section after
 *  the section with the given id (title). */
export function addSectAfterSection(id: string | null): void {
  console.log('[action] add-section after section', id);
  // TODO: create new MnHdg section after this section
}
