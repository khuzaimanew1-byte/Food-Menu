Is turn mein image upload nahi hui — sirf text mention hai. Prompt update kar deta hoon generic reference ke sath, lekin actual image dekhne ke baad style-matching aur precise ho sakegi (upload kar do agla message mein to main deeply analyze kar ke isse bhi refine kar dunga).
Updated prompt:
Build a multi-page digital restaurant menu web app called "Infinity Castle's Cuisine" in React. Visual language: dark charcoal background, gold (#d4af37) ornate borders, Playfair Display for headings/border medallion, Cormorant Garamond for item names/prices, Jost for descriptions — Infinity Castle / Demon Slayer aesthetic, matching attached cover/closing references. A separate main-menu reference image is also provided — match its formatting cues (spacing rhythm, item-row proportions, decorative accents) and let item styling lean closer to it where it differs from the current MenuItem.jsx draft, without breaking the established gold/charcoal/typography system.
Typography: All font imports/declarations live in one dedicated typography.css file (Google Fonts @import + reusable font-family/weight/size classes or CSS variables), loaded once globally. No component or other style file may declare/import a font directly — strip any existing font <link>/font-family rules from provided files and route through this single file, so fonts stay consistent and non-repeated app-wide.
Stage/A4 separation: One top-level "stage" fills the viewport and centers a single A4-ratio (210:297) page box at a time. The A4 box contains nothing but printable content (border, heading, items) — no buttons/indicators/nav inside it, ever. All chrome (print button, nav controls, total indicator) are fixed-position siblings of the A4 box, direct children of the stage, sized in viewport units so they never scale with the A4 box. A4 box internally uses only container-query units, independent of stage sizing.
Reused components (unmodified): MenuBorder (gold frame + corners + optional medallion via pg prop) and MenuHeading (section titles). Apply MenuBorder on every page: pg={false} on Cover/Closing (frame only, no medallion), pg={pageNumber} on content pages.
Cover/Closing: Static image-based components rendering the provided reference images inside MenuBorder — not recreated in CSS.
ContentPage: Composes MenuBorder + MenuHeading + item list. Takes a layout prop ("single"/"two-column") that only toggles a CSS class — layouts are pure CSS in the layout styles file, not separate component files.
Item components: Extract ItemAvatar (thumbnail) as standalone. Number badge is a diamond — 45°-rotated bordered square with gradient fill, number counter-rotated upright — not circular, unless the main-menu reference image shows a different badge shape, in which case match that instead. MenuItemCard imports ItemAvatar + number badge internally, plus name, a single fading-gradient leader-line, gold-gradient text-clip price, and description, adjusted to visually align with the main-menu reference's item formatting. No page-level logic inside it.
Scroll vs print: A4 box scrolls internally on screen (overflow-y: auto) when content overflows; a print media query resets it to overflow: visible; height: auto so nothing clips when printed.
Navigation: One reusable NavButton (direction prop: "prev"/"next", single shared style, icon flips). NavControls is one component, fixed bottom-center of stage, holding its own current-page state and goNext/goPrev handlers inline (no separate hook file), rendering two NavButtons. Disable Prev on first page, Next on last. No "current/total" text here.
Total indicator: Fixed bottom-right of stage, shows only a total count, visible only on content pages (hidden on Cover/Closing), never part of the A4 box.
Print button: Small gold-outlined icon button, fixed top-right of stage, triggers browser print.
Print isolation: Every chrome element gets a shared no-print class; A4 box gets a print-area class, so @media print isolates just the printable page, no stray blank pages/margins.
Styles: Besides typography.css, a dedicated layout styles file holds all spacing/layout rules including single/two-column variants.
App.jsx: Holds page sequence (Cover → ContentPage instances → Closing) and current index state, renders active page inside the stage, mounts NavControls/PrintButton/total indicator globally at stage level.