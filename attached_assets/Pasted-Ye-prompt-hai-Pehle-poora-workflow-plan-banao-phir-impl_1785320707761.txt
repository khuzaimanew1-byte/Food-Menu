Ye prompt hai
Pehle poora workflow plan banao phir implementation shuru karo.
Jobhi files tum read aur ussmein jo understand kar rahe ho wo bhi likhna, saari details likhna,   jo jo karoge wo likhna kuch miss nhin karna.
And Write what you read analyze and understand in file, draft a step-by-step workflow, then before each step write your understanding of it, current step, and step being started.
agar saare employee delete ho jaayein no assign employee likha ho normal mode mein edit mein + icon ho. samjhe,
Ye analysis hai assign modal ka iss create karo jab item area pe context menu pe assign pe click karein toh modal aaye.
dot indicator reusable ho.
File padh li. Ab codebase ke relevant parts explore karta hoon — context menu ka assign option, dropdown system, aur existing data models — sab parallel mein:


3 actions
Ab relevant files directly padhunga — assign action, ContextMenu config, DropdownPanel, aur MnItm:


8 actions
Ab main poora analysis likhta hoon — file kya keh rahi hai, codebase mein kya mila, aur dono ko jod ke kya build karna hai.

Jo file padhi — uska har hissa
File ka title/context (line 1–4):

"Ye prompt hai. Abhi kuch change nhin karo sirf analyze karo. Context menu mein jo assign option hai usse update karo."

Matlab yeh ek build prompt hai Assign feature ke liye. Abhi sirf analysis chahiye, implementation nahi.

Section 1 — Assign Indicator (dot on item card)
File kya kehti hai:

Item card ke image ke bottom-right corner pe ek tiny dot lagega
Dot = assignment status indicator (assigned hai ya nahi)
Agar kisi item ka koi Assign record hi nahi bana → dot bilkul nahi dikhega (hidden nahi, DOM mein hi nahi hoga)
Dot badge ya label nahi, sirf ek colored circle
Codebase mein kya mila: MnItm.tsx (line 144–156) padha. Item card ka structure yeh hai:

div.mic-wpr
  └── div.mic
        ├── div.mic-avt          ← yahan avatar hai (Avt component)
        └── div.mic-body         ← name, price, desc

mic-avt div ke andar Avt component render hota hai jo shape prop leta hai (ic, sq, plq). Dot yahan mic-avt ke bottom-right pe position:absolute se lagega — Avt component ke upar overlay ke taur pe. Abhi koi dot nahi hai.

Section 2 — Assign Modal
File kya kehti hai: Modal ka base BsMdl hoga (jo humne banaya). Do modes hain:

Normal Mode (jab data pehle se exist kare):

Header mein item ka naam (quiet/small)
Top-right: ✎ (edit icon) + ✕ (close)
Employees + unke roles: paired layout, wrap karte hain
Resources + qty + unit: paired layout, wrap karte hain
Margin: bottom-right, Margin: Rs.{value}
Data nahi → "Not assigned" text
Edit Mode (pehli baar open hone pe default; ya ✎ click pe):

Top-right: ✓ (save/confirm) + ✕ (cancel)
Poore modal ko ek edit-highlight border/glow milega (per-field nahi, sirf outer wrapper)
Defaults: employee name empty, role unselected, resource name empty, qty = 0, unit = default, Margin = Rs.0
Qty = numeric input only
Role aur Unit = EdtDrp (naya dropdown component, sirf ▾ icon trigger)
✓ confirm button disabled rahega jab tak koi bhi Resources field na bhari ho (Assign/employee fields zaroori nahi)
Cancel karo pehle save se pehle → koi Assign record nahi banta, dot nahi dikhta
ASCII wireframes se layout clear hua:

Edit Mode (first time, no existing data):

Item Name                        ✓  ✕
Assign:
[Emp name field]   [Chef ▾]   [+]
Resources:
[resource field]   [0] [g▾]   [+]
                          Margin: Rs.0

Normal Mode (data exists):

Shawarma                         ✎  ✕
Assign:
Ahmad    chef    ali    barista
Resources:
Chicken  250g    Oil    20ml
Salt     5g
                          Margin: Rs.50

Edit Mode (existing data):

Shawarma                         ✓  ✕
Assign:
Ahmad   [chef▾]  ali   [barista▾]
[+]
Resources:
Chicken  [250] [g▾]    Oil    [20] [ml▾]
Salt     [5]   [g▾]
                        [+]
                          Margin: Rs.50

Codebase mein kya mila:

BsMdl humne banaya hai → bs-hdr, bs-bdy, bs-crown, animations sab ready hain
BsMdl ka X button pehle se cancel ka kaam karta hai (humne design mein yeh rakha tha)
Assign Modal BsMdl ko extend karega apni internal mode-state ke saath
Section 3 — EdtDrp (Edit Dropdown)
File kya kehti hai:

Existing DropdownPanel/OptRow pe banaya naya component
Role aur Unit fields ke liye
Trigger = sirf ▾ icon (koi border, box, outline nahi)
Har option ke upar per-option context menu hoga (long-press/right-click/hover — existing pattern match karo)
Context menu mein ek entry: Edit
Edit select karo → woh option label inline editable ho jata hai
Rename globally update hoga (roles/units DB tables se linked hain)
Codebase mein kya mila: DropdownPanel.tsx padha (141 lines). Aaj tak yeh sirf ContextMenu.tsx use karta hai. Lekin:

OptRow pehle se exported hai (line 35) — EdtDrp mein reuse ho sakta hai
CtxOpt interface pehle se exist karta hai — role/unit options usi format mein store ho sakte hain
DropdownPanel ko left, top, menuRef, subMenuRef chahiye — yeh currently absolute-positioned panel hai
EdtDrp ek wrapper hoga jo trigger (▾) + panel positioning + per-option context menu handle kare
Per-option context menu ke liye: ContextMenu.tsx ka existing pattern match karna hoga (right-click/long-press detection) — duplicate system nahi banana.

Section 4 — Visual Style
File kya kehti hai:

Infinity Castle theme extend karo — "dark obsidian surfaces, subtle indigo-violet gradients"
Sharp geometric linework (rounded/skeuomorphic nahi)
Existing accent = gold (--gold, --gl) — edit-highlight, ✓ confirm, ▾ icon ke liye
Edit-mode border: subtle glow
Text hierarchy: primary (item name), secondary (role, qty/unit), tertiary (margin)
Codebase mein kya mila: variables.css padha. Indigo/violet ke liye koi existing token nahi — yeh prompt mein mention hai lekin design tokens nahi hain. Matlab ya toh:

Existing dark tokens (--ink, --chrd, --chr) hi "obsidian" hain — wo already dark dark hain
Subtle violet tint sirf BsMdl ke radial-gradient mein add karna hoga (nayi CSS var nahi)
Gold tokens already available: --gold, --gl, --gm, --gw, --lntn (lantern amber)

Bottom section — Row Rules & Delete Logic
File kya kehti hai (last 7 lines):

Block	Min rows	Delete icon kab dikhega
Employees	0 (sab delete ho sakte hain)	Hamesha (last row pe bhi)
Resources	1 (ek row zaroori)	Sirf jab 2+ rows hon; last row pe hidden
+ button naya row add karta hai — default: name empty, role/unit unselected, qty = 0
Role aur Unit alag-alag DB tables hain (roles, units) — rename karo toh globally update hoga
EdtDrp ka Delete action: codebase mein jo existing "delete with in-use check + confirmation" pattern hai wahi reuse karo — naya pattern mat banao
Codebase mein kya mila: delExec.ts padha — existing delete pattern: pushToTrash → apiDel → store.del. Yahi pattern EdtDrp ke "Delete option" ke liye reuse hona chahiye. DelCnf → CnfMdl → BsMdl chain bhi available hai.

Kya build karna hoga — Workflow
Maine codebase se confirm kiya:

1. assign.ts stub hai — sirf void id hai, koi logic nahi. Context menu wiring ready hai (dispatchCtxAction → assignItem). Bas yahan se event dispatch karna hoga.

2. Data stores nahi hain — assignStore, roleStore, unitStore — teen nayi files banana hogi.

3. EdtDrp — naya component, DropdownPanel/OptRow pe. Abhi DropdownPanel sirf ContextMenu use karta hai lekin OptRow export hai toh reuse hoga.

4. AsgMdl (Assign Modal) — BsMdl extend karega. Mode state internal rahegi (normal/edit). Edit-highlight poore bs-dlg pe lagega, per-field nahi.

5. AsgDot — MnItm ke mic-avt area mein position: absolute; bottom: X; right: X pe tiny dot.

6. roles aur units tables — DB mein exist karni chahiye. Rename = PATCH /api/roles/:id ya PATCH /api/units/:id — API server se confirm karna hoga.

Summary — Meri complete understanding
Prompt ek complete Assign feature describe karta hai jisme:

Item card pe dot indicator (assignment status)
Assign Modal (BsMdl pe built, normal + edit modes)
EdtDrp (inline-editable dropdown for Role/Unit, DropdownPanel pe built)
Data layer (assign, roles, units stores)
Delete reuse pattern (existing DelCnf chain)